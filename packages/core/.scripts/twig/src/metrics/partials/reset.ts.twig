

reset(options?: MetricOptions): void {
  const {adapter, metric} = {...(options || {})} as MetricOptions;
  this.{{metricType}}Adapters(adapter, metric).forEach(({{metricType}}) => {
    {{metricType}}.reset();
  });
}
